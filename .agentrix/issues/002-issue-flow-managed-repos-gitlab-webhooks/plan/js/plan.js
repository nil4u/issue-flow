const details={happy:{title:'正常自动化路径',body:'GitLab webhook 进入 issue-flow Gateway，校验 secret 后记录 delivery；Normalizer 产生 issue-flow event context；Dispatcher 使用保存的仓库 token 调 GitLab API，并在需要时调用 Agentrix 创建/恢复任务。'},duplicate:{title:'重复投递路径',body:'相同 delivery id 或 payload hash 再次到达时，WebhookDeliveryConsumer 命中，直接返回已处理/duplicate；不会重复创建评论、label、MR review 或 Agentrix task。'},token:{title:'Token 失效路径',body:'GitLab API 返回 401/403 时，DispatchRun 记录 sanitized error，仓库详情页进入 token action required；不把 token 写入日志、前端或 Agentrix task env。'},compat:{title:'兼容 CI bridge 路径',body:'已安装旧 GitLab CI workflow 的仓库可以继续由 bridge 变量触发 pipeline；新仓默认 server-side dispatch。兼容模式用于逐仓迁移和回滚，不作为主路径。'}};const map=document.querySelector('.runtime-map');const box=document.getElementById('path-detail');document.querySelectorAll('.path-tab').forEach(btn=>{btn.addEventListener('click',()=>{document.querySelectorAll('.path-tab').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const key=btn.dataset.path;map.dataset.path=key;box.innerHTML=`<h3>${details[key].title}</h3><p>${details[key].body}</p>`})});map.dataset.path='happy';
